services:
  app:
    build:
      context: ../../
      dockerfile: infra/docker/Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - ../../:/app
      - /app/node_modules
    command: npm run start:dev
